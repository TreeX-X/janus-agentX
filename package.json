{
  "name": "janus-agentx",
  "version": "0.1.0",
  "private": true,
  "description": "Janus agent engine extracted from JanusX - agent loop, runtime, LLM orchestration, knowledge, roundtable and blueprint maintenance, with standalone janus CLI.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "MIT"
}
